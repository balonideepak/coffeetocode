package scjp.polymorphism;

public class Child extends Parent {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		Parent obj = new Child();
		obj.callMe(); // If method is overriden in child class then at run-time  child class method will be called . In case method is
							// not provided in child class then parent class method  will be called with no compilation error as methods  are binded at compile time to reference variable
	}

	// public void callMe() {
	// System.out.println("Called from Child class");
	// }

}
